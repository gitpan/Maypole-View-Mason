<%doc>
=head2 button

This is a generic button, which performs an action on an object.

=cut

</%doc>

<td>
<form method="post" action="<%$base%>/<%$item->table%>/<%
  $config->{request_reverse} ?
  "$action/".$item->id :
  $item->id. "/$action" 
%>">
    <input type="submit" name="<%$action%>" value="<%$action%>">
</form>
</td>

<%args>
$config
$base
$item
$action
</%args>
